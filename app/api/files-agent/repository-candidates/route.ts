import { NextRequest,NextResponse } from 'next/server'; import { bearerSecret,boundedAgentJson,filesAgentHttpError,FilesAgentError } from '@/lib/files-agent'; import { publishRepositoryCandidates } from '@/lib/tracemini-discovery';
export async function POST(req:NextRequest){try{const c=bearerSecret(req);if(!c)throw new FilesAgentError('Bearer device credential required',401);const b=await boundedAgentJson(req);return NextResponse.json({ok:true,...await publishRepositoryCandidates(c,b)},{headers:{'cache-control':'no-store'}})}catch(e){const x=filesAgentHttpError(e);return NextResponse.json({ok:false,error:x.message},{status:x.status,headers:{'cache-control':'no-store'}})}}
